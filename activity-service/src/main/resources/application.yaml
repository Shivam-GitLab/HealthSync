spring:
  application:
    name: product-service
  mongodb:
    uri: mongodb://${MONGO_HOST:localhost}:${MONGO_PORT:27017}/healthsync_product

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: ${SERVER_PORT:8082}